import { action } from "@storybook/addon-actions";
import { ToggleGroupComponent } from "../components/ToggleGroup";

export default {
  title: "Stories/ToggleGroupComponent",
  component: ToggleGroupComponent,
  parameters: {
    layout: "centered",
  },
  args: {},
};

export const Primary = {
  args: {},
};
